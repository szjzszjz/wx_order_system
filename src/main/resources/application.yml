spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://192.168.1.151/sell?characterEncoding=utf-8&useSSL=false&useTimezone=true
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    show-sql: true

  jackson:
    default-property-inclusion: non_null #全局配置 若返回字段为null 则不返回给前端

server:
  servlet:
    context-path: /sell

#日志输出的格式
#logging:
#  pattern:
#    console: "%d - %msg%n"
##  path: /var/log/tomcat/
#file: /var/log/tomcat/sell.log